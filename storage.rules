rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Item images: anyone can read; only signed-in users can upload/delete.
    match /items/{file} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
